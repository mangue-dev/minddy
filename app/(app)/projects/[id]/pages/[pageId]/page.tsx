export default function ProjectPageDetail() {
  // PagesShell owns the detail surface so document-to-document navigation can
  // stay inside one persistent client router without waiting for a new RSC tree.
  return null;
}
