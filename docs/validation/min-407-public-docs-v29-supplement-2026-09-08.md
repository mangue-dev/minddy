# Public v0.10.29 replay supplement

The primary sealed phase report remains unchanged. This supplement corrects its integration-attribution observation: the triage list shows a blue integration icon, hovering exposes “Created via the Clean room caller integration”, and the selected issue detail Activity states “Integration Clean room caller created the issue”. Screenshots and the tooltip snapshot demonstrate both surfaces. The earlier text-only, early snapshot was insufficient evidence for a missing attribution badge. Database attribution and effective UI attribution both exist; no attribution gap is established.

A fresh isolated browser context used the standard Playwright `browser.contextOptions.recordHar` configuration, mode `full`, content `omit`, without a URL filter. Fresh password and TOTP sign-in succeeded, followed by ROOM-1 readback and its 25-byte attachment preview (`clean-room-storage-marker`), and ROOM-2 triage/Activity readback. Recording captured 497 requests from 2026-09-08T18:58:27.571Z through 19:02:29.227Z: 494 status 200 and 3 status 101, all hostname localhost. These observations cover this browser context and interval only, not Chromium background traffic, npm, server-side egress, or unexercised features.

The CLI's ordinary `close` initially did not produce a HAR. One bounded smoke check confirmed that the documented standard `await page.context().close()` flushes it, then the complete fresh-login recording was repeated with that close. No custom network event hooks or product modifications were used. The protected private HAR was parsed locally into only hostname, method, status and request timestamp, then deleted at 2026-09-08T19:03:04.512010Z. The summary verifies it is absent. HAR headers/cookies/payloads and full URLs are not included.

Public tooling references: https://github.com/microsoft/playwright-cli (configuration and contextOptions), https://github.com/microsoft/playwright/blob/main/docs/src/api/params.md (recordHar, content omission, context close requirement). Product steps remain constrained to the published v0.10.29 source and live public self-hosting guide. The unchanged application, environment, image and data remain available for later lifecycle checks. Supplement prepared 2026-09-08; file timestamps and recording start/end files preserve the bounded intervals. `har-start.txt`/`har-end.txt` concern the initial unsuccessful recording; `har-verified-*` identify the successful repeat.
