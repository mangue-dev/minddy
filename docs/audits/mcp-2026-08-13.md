# Audit MCP — 2026-08-13

## Bloquant

None.

## Drift

- The MCP only allowed `allow_comments` to be set on the feedback board. The app also exposes `show_categories`, `show_views` and `visible_view_ids` in `app/api/projects/[id]/feedback/settings/route.ts:107-158`, and Numo announces them in `lib/server/assistant/tools.ts:936-956`. The MCP schema and handler of `lib/server/mcp/tools.ts:3945-4010` now expose them; View identifiers are filtered to the project's shared views before writing.
- The description of the `url` parameter of `minddy_configure_webhook` announced an HTTPS URL only in `lib/server/mcp/tools.ts:4143-4149`, while the business contract explicitly accepts `http:` or `https:` in `lib/server/integrations.ts:132-148`. The description now says `http(s)`.

## Cosmetics

None.
