# Audit MCP — 2026-08-13

## Bloquant

Aucun.

## Dérive

- Le MCP ne permettait de régler que `allow_comments` sur le board de feedback. L'application expose aussi `show_categories`, `show_views` et `visible_view_ids` dans `app/api/projects/[id]/feedback/settings/route.ts:107-158`, et Numo les annonce dans `lib/server/assistant/tools.ts:936-956`. Le schéma et le handler MCP de `lib/server/mcp/tools.ts:3945-4010` les exposent désormais; les identifiants de vues sont filtrés aux vues partagées du projet avant écriture.
- La description du paramètre `url` de `minddy_configure_webhook` annonçait une URL HTTPS uniquement dans `lib/server/mcp/tools.ts:4143-4149`, alors que le contrat métier accepte explicitement `http:` ou `https:` dans `lib/server/integrations.ts:132-148`. La description annonce désormais `http(s)`.

## Cosmétique

Aucun.
