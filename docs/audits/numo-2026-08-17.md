# Audit Numo — 2026-08-17

## Dérive

- `add_feedback_comment` existait côté MCP mais n'était pas exposé ni exécuté par Numo. Il est désormais déclaré dans `lib/server/assistant/tools.ts`, exécuté par `lib/server/assistant/execute-tool.ts` avec une visibilité interne forcée, et décrit dans `lib/server/assistant/prompt.ts`.
- `smart_fill` était déclaré dans les champs de `update_issues`, mais le noyau `updateIssueFields` l'ignore. Il reste exposé uniquement sur `create_issue`, où `createIssueForProject` le traite réellement.

## Vérification

- Contrats vérifiés dans `lib/server/assistant/tools-contract.test.ts`.
- Le contrat des outils et le catalogue des labels du fil couvrent le nouveau tool Numo ; aucun outil ne pointe vers un code supprimé ou une énumération obsolète dans le périmètre audité.
