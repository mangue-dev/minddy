# Audit Numo — 2026-08-17

## Drift

- `add_feedback_comment` existed on the MCP side but was not exposed or executed by Numo. It is now declared in `lib/server/assistant/tools.ts`, executed by `lib/server/assistant/execute-tool.ts` with forced internal visibility, and described in `lib/server/assistant/prompt.ts`.
- `smart_fill` was declared in the `update_issues` fields, but the `updateIssueFields` kernel ignores it. It remains exposed only on `create_issue`, where `createIssueForProject` actually processes it.

## Verification

- Contracts checked in `lib/server/assistant/tools-contract.test.ts`.
- The tools contract and the thread label catalog cover the new Numo tool; no tool points to deleted code or obsolete enumeration in the audited scope.
