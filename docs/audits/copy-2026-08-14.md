# Audit copy — 2026-08-14

Fenêtre analysée : 2026-08-07 → 2026-08-14 (aucun rapport `copy-*.md` antérieur, retour à 7 jours).
Périmètre : `messages/fr.json` et `messages/en.json`, plus les chaînes en dur de `app/` et `components/` vérifiées pour les règles dures.

## Règles dures

**Em-dashes (U+2014) retirés : 17** (8 en FR, 9 en EN). Après correction, 0 dans les deux catalogues.
**Tutoiements corrigés : 1.** Tous les autres marqueurs de tutoiement restants sont des gabarits de prompt **utilisateur → agent** (namespace `launchPrompt`, `emptyNumoPrompt`, `wizardSeedNumoPrompt`, `syncPromptText`, `reviewFindingsPrompt`, `assistantPrompt`, exemples de routines, `Seed.createdReply`) : registre correct, non touchés.

### Em-dashes corrigés

| Clé | Avant | Après |
| --- | --- | --- |
| `Auth.errorWeakPassword` | Ce mot de passe est trop courant — choisissez-en un moins prévisible. | Ce mot de passe est trop courant : choisissez-en un moins prévisible. |
| `IssueUI.smartFillOffHint` | Coupé pour ce ticket — vous renseignez ses propriétés vous-même. | Coupé pour ce ticket : vous renseignez ses propriétés vous-même. |
| `Account.sendShortcutDesc` | ...une description — un nouveau ticket, un nouvel objectif — restent sur {mod} + Entrée | ...une description (un nouveau ticket, un nouvel objectif) restent sur {mod} + Entrée |
| `Agent.errorCheckpointRefused` | ...sur la branche — envoyez un message pour poursuivre. | ...sur la branche : envoyez un message pour poursuivre. |
| `Admin.fields.recommended_models.desc` | ...le moins cher d'abord — vous ne choisissez donc QUE... | ...le moins cher d'abord : vous ne choisissez donc QUE... |
| `Admin.fields.smart_fill_model.desc` (EN seule) | Someone is waiting on it — pick a fast model. | Someone is waiting on it: pick a fast model. |
| `Download.metaTitle` | minddy pour Mac — l'application de bureau | minddy pour Mac : l'application de bureau |
| `Download.platformNote` | macOS uniquement pour l'instant — il n'y a pas... | macOS uniquement pour l'instant : il n'y a pas... |

Mêmes clés corrigées côté `en.json` (dont `recommended_models.desc` : « everywhere in the app — before anyone types » → « …, before anyone types »).

### Tutoiement corrigé

| Clé | Avant | Après |
| --- | --- | --- |
| `Inbox.lineAutomationPaused` | L'automatisation attend ton feu vert | L'automatisation attend votre feu vert |

## Par nature — corrections de fenêtre

**Ponctuation (incise après passage em-dash → « ; »** la précédente passe mécanique avait transformé les tirets en « ; » d'affilée) :
- `Settings.gitConnectionsHelp` (FR + EN) : « sur une pull request; approuver, commenter, résoudre un fil, fusionner; sous votre nom » → « sur une pull request (approuver, commenter, résoudre un fil, fusionner) sous votre nom ». Les parenthèses portent seules l'apposition.

**Rien d'autre à corriger** dans la fenêtre : aucune faute de sens, de grammaire ou de vocabulaire relevée sur le nouveau contenu (pages, smart-fill, ajouts des réglages, etc.).

## Laissé en l'état, par arbitrage produit

- Les gabarits de prompt en `;` doubles (`Agent.launchPrompt` `verifyImplementation` / `chainVerifyImplementation`, FR + EN : « ...avait été demandé; le ticket, son plan et ses commentaires...; puis confronte-les ») : lisibles dans une consigne, non touchés.
- Le tiret em-dash comme placeholder de valeur vide (`"—"`) dans plusieurs composants : convention typographique d'affichage « aucune valeur », préexistante et hors fenêtre, pas de la prose. À trancher si la règle doit l'exclure aussi.
