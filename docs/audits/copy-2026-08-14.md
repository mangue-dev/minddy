# Audit copy — 2026-08-14

Window analyzed: 2026-08-07 → 2026-08-14 (no previous `copy-*.md` report, return to 7 days).
Scope: `messages/fr.json` and `messages/en.json`, plus hard strings of `app/` and `components/` checked for hard rules.

## Hard rules

**Em-dashes (U+2014) removed: 17** (8 in FR, 9 in EN). After correction, 0 in both catalogs.
**Corrected familiarity: 1.** All other remaining familiarity markers are **user → agent** prompt templates (namespace `launchPrompt`, `emptyNumoPrompt`, `wizardSeedNumoPrompt`, `syncPromptText`, `reviewFindingsPrompt`, `assistantPrompt`, example routines, `Seed.createdReply`): correct register, not affected.

### Em-dashes fixed

| Key | Before | After |
| --- | --- | --- |
| `Auth.errorWeakPassword` | This password is too common — choose one that is less predictable. | This password is too common: choose one that is less predictable. |
| `IssueUI.smartFillOffHint` | Cut for this ticket — you fill in its properties yourself. | Cut for this ticket: you enter its properties yourself. |
| `Account.sendShortcutDesc` | ...a description — a new ticket, a new objective — stays on {mod} + Enter | ...a description (a new ticket, a new objective) remains on {mod} + Enter |
| `Agent.errorCheckpointRefused` | ...on the branch — send a message to continue. | ...on the branch: send a message to continue. |
| `Admin.fields.recommended_models.desc` | ...cheapest first — so you ONLY choose... | ...cheapest first: so you ONLY choose... |
| `Admin.fields.smart_fill_model.desc` (EN only) | Someone is waiting on it — pick a fast model. | Someone is waiting on it: pick a fast model. |
| `Download.metaTitle` | minddy for Mac — the desktop application | minddy for Mac: the desktop application |
| `Download.platformNote` | macOS only for now — there is no... | macOS only for now: there is no... |

Same keys corrected on the `en.json` side (including `recommended_models.desc`: “everywhere in the app — before anyone types” → “…, before anyone types”).

### Tutorial corrected

| Key | Before | After |
| --- | --- | --- |
| `Inbox.lineAutomationPaused` | Automation awaits your green light | Automation awaits your green light |

## By nature — window fixes

**Punctuation (incise after pass em-dash → “;”** the previous mechanical pass had transformed the dashes into “;” in a row):
- `Settings.gitConnectionsHelp` (FR + EN): “on a pull request; approve, comment, resolve thread, merge; under your name” → “on a pull request (approve, comment, resolve thread, merge) under your name”. The parentheses alone bear the affix.

**Nothing else to correct** in the window: no errors in meaning, grammar or vocabulary noted on the new content (pages, smart-fill, additions of settings, etc.).

## Left as is, by product arbitration

- The double `;` prompt templates (`Agent.launchPrompt` `verifyImplementation` / `chainVerifyImplementation`, FR + EN: “...had been requested; the ticket, its plan and its comments...; then compare them”): readable in an instruction, not touched.
- The em-dash as an empty value placeholder (`"—"`) in several components: “no value” display typographic convention, pre-existing and out of window, not prose. To be decided whether the rule should also exclude it.
